#!/bin/bash
set -e

[[ -z $IMAGE ]] && IMAGE="mauchede/aria2"
[[ -z $TAG ]] && TAG="1.18.8"

docker run \
    -e HOME=$HOME \
    -i \
    --net host \
    --rm \
    -t=$([ -t 0 ] && echo "true" || echo "false") \
    -u $UID \
    -v $HOME:$HOME \
    $IMAGE:$TAG "$@"
